@startuml sequence-diagram

header Massage Reservation Sequence Diagram
title "Massage Reservation (GET)"

participant "Client" as client
participant "<<javaScript>>\n:server" as server
participant "<<router>>\n:reservations" as routerReservations
participant "<<middleware>>\n:protect" as middlewareProtect
participant "<<controllers>>\n:reservations" as controllersReservations
participant "<<model>>\n:Reservation" as modelReservation
database "<<MongoDB>>\n:reservations" as reservationsDatabase

== 7. Admin view any massage reservation. ==

client -> server ++: req.get('/api/v1/reservations')
server -> routerReservations ++: app.use('/api/v1/reservations', reservations)
routerReservations -> middlewareProtect ++: protect()
middlewareProtect --> routerReservations --: next()
routerReservations -> controllersReservations ++: getReservations()
note right of controllersReservations: role == admin\nreqQuery = {}
controllersReservations -> modelReservation ++: find(reqQuery).populate(...) (populate shop and service)
modelReservation -> reservationsDatabase ++: ReservationSchema
reservationsDatabase --> modelReservation --: reservations
controllersReservations <-- modelReservation --: reservations
controllersReservations -> client --: response

@enduml
